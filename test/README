1) To build all the test code run
make

2) To run the tests after it has been built:
    run the executable runAll (use --help to choose parameters)

2) To build and run all the tests at once in default mode use: 
make run

3) Before making/running tests, ensure that 
   the source code in ryggrad has been built.


